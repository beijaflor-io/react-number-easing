{
  "name": "react-number-easing",
  "version": "0.0.4",
  "description": "React component for fancy transition for numbers.",
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "mkdir -p dist && babel --presets react --plugins transform-object-rest-spread src/index.jsx -u -o dist/index.js --stage 1",
    "pages": "git subtree push --prefix gh-pages origin gh-pages",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "react",
    "react-component",
    "component",
    "number",
    "easing"
  ],
  "author": "javierbyte",
  "license": "ISC",
  "dependencies": {
    "eases": "^1.0.6"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-react": "^6.24.1"
  }
}
